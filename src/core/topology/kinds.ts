export type EditMode = "object" | "vertex" | "edge" | "face";
export type OID = number; // object id in a SceneGraph (placeholder until scene system exists)
export type VID = number;
export type HEID = number; // canonical edge id should be the lower half-edge id of a twin pair
export type FID = number;
